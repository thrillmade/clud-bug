← back to [docs/timeline.md](../timeline.md)

## 2026-09-15 14:45 - Build harness attestation (#266 PR1, SPEC §4.4/§4.5/§8.1): the harness records which reviewing agents ran; the bundle carries the record; post-check-run re-derives it at the boundary

**Reasoning:** SPEC 2.0 §4.4 requires, per reviewing agent, the harness's identifier, role, RESOLVED model, resolved effort, the dispatching call and a lens label/digest — never prompt text, never a network call — registered in the repository's committed harness settings; §4.5 says a notary must read it from the check itself, never accept one handed over by the reviewing party; §8.1 says the force is tamper-evidence, not tamper-proofness. Claude Code's SubagentStop event carries no model and subagents launch in the background, so the design records TWO rows joined on agent_id: PostToolUse(Agent) writes the dispatch row (tool_response.resolvedModel, tool_use_id, description as lens label, sha256 of the prompt as digest) and SubagentStop (matcher clud-bug-reviewer) writes the completion row. Both hooks are async, always exit 0, inline node, no npx/network; the store is <git-common-dir>/clud-bug-attest.jsonl, never in the reviewed tree. Registration goes through the existing mergeLocalReviewHook, generalised per event and preserving foreign hooks; init and update write .claude/agents/clud-bug-reviewer.md (no model pin — every dispatch names the model) and the recipe dispatches reviewers as that type. src/core/attestation.ts joins and filters by head_sha, caps at 32, stamps store: read|absent|unreadable and registration: committable|uncommittable (git check-ignore — the tamper-evidence claim holds only for a committed registration, and the CLI now warns when it cannot be). NotaryBundle goes to version 2 with an optional attestation field; a malformed attestation degrades to absent-with-reason, never a null bundle; post-check-run discards any caller-supplied attestation and re-derives from the store (a forged bundle was submitted in the boot and the POSTed body carried only the harness's record). No producer_claim, no self-reviewed (#246 owns it), nothing about adversarially-reviewed (#336). Five refute lenses + a final fixer; 14 named mutations proven in throwaway worktrees; booted with a real init and real hook payloads; 58 files / 1310 tests green in the lane tree. The App half (predicate + base-ref registration read + independence label) is PR2, after this ships in an rc.

**Alternatives considered:** Any cryptographic signing (rejected: §8.1 wants a committed registration, not keys), Parse agent_transcript_path for the model (rejected: undocumented schema; resolvedModel is on the dispatch event), Emit producer_claim: self-reviewed here (rejected: #246's live ruling owns it; the predicate does not depend on it)

**Implications:**
- The App must bump its clud-bug pin to an rc carrying bundle v2 before it can read the field; until then it fails closed to independence-unestablished
- A repo whose .claude/ is gitignored gets a loud warning and registration: uncommittable in its bundle — the notary must never certify that as independently reviewed
- adversarially-reviewed still needs per-lens hunk coverage (#336)

---

