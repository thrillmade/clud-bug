{
  "id": "b11-crosscutting-error-contract",
  "class": "cross-cutting",
  "severity": "MED-HIGH",
  "expected": "finding",
  "file": "module.mjs",
  "lineRange": [
    24,
    32
  ],
  "acceptAlso": [
    {
      "file": "validate.mjs",
      "lineRange": [
        12,
        17
      ]
    }
  ],
  "defect": "normalizeTransfers guards account resolution with try/catch, assuming parseAccountId() throws on a malformed reference, but the helper returns null on failure — so the guard never fires and an unresolved { to: null } transfer slips into `accepted` and is applied to the ledger."
}
