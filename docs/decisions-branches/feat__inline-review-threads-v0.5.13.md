## 2026-05-26 13:10 - Instruct bot to post inline review threads (v0.5.13) — close the conversation-resolution loop

**Reasoning:** Discovered the gate-side counterpart to v0.5.12: the reporulez clud-bug-logmind variant ALREADY enables required_review_thread_resolution via the pull_request rule parameters (verified live on this repo), but clud-bug has never produced inline review threads for the gate to operate on. Pre-v0.5.13 prompt explicitly told the bot to "post your review as a single PR comment" with a buried weak mention of the mcp__github_inline_comment__create_inline_comment tool. Effect: every review across every install posted top-level PR comments (not gateable) and zero inline review threads. The README, v0.5.4 status block "resolved from prior" counter, and the entire fix-and-resolve loop have been a documented-but-silent-no-op since launch. v0.5.13 restructures the prompt to make inline review threads the PRIMARY surface for each finding (one thread per finding anchored to file:line via the MCP tool); summary PR comment becomes the secondary surface that the strict-mode gate still reads for the H2 header. Fix-push flow tightened: prompt explicitly tells the bot to list prior claude[bot] threads and resolve those whose issues are verifiably fixed in head diff, BEFORE posting the new review.

**Alternatives considered:** Add required_review_thread_resolution to the ruleset. Rejected: not needed — reporulez clud-bug-logmind variant already has it as a parameter inside the pull_request rule (had to look at the rule parameters; not a separate top-level rule). The gate is already armed; the prompt is the only side that needed fixing. Also considered: ship as a v0.6 prompt-system overhaul. Rejected: this fix is small + obviously correct + dramatically improves the per-PR signal-to-noise. No reason to defer for a bigger refactor.

**Implications:**
- First PR after v0.5.13 ships should produce N inline review threads (one per finding) that must be resolved to merge — the gate that was sitting idle since v0.5.4 actually engages. Status block "resolved from prior" counter now has real data on fix-push reviews. Existing v6 installs auto-upgrade to v7 via refresh-mode. v0.5.13 + v0.5.12 together close out the entire BB-stream + status-block correctness regression that dogfooding PR #59 surfaced.

---
