// Generated by scripts/render-benchmark.mjs from benchmark/results/latest.json.
// Do not edit by hand — `node scripts/check-benchmark-claims.mjs` fails CI on drift.
//
// Both benchmark passages on the site read every number from here, so the
// front page cannot claim a score the last run did not produce.

export const BENCHMARK = {
  scenarios: 20,
  corpusPlanted: 14,
  corpusDecoys: 6,
  planted: 14,
  caught: 14,
  recallPct: 100,
  decoys: 6,
  decoysClean: 6,
  falsePositives: 0,
  precisionPct: 100,
  unverified: 0,
  unverifiedClause: "",
  reviewersPerScenario: 3,
  reviewersPhrase: "3 independent reviewers",
  model: "model unrecorded",
  effort: "unrecorded",
  recipeSha: "unrecorded",
  invocation: "manual panel (pre-automation)",
  runDate: "2026-07-03",
  costLine: "spend not metered",
  caveat: "The recipe that produced this score was not recorded, so it cannot be matched to the one shipping today — read it as a historical measurement, not a reproducible claim.",
} as const;

export const BENCHMARK_SENTENCE =
  "Planted-defect benchmark — regression corpus. On the 20-scenario committed corpus (model unrecorded, effort unrecorded, recipe unrecorded, manual panel (pre-automation)), clud-bug caught 14/14 planted defects (100%) and false-flagged 0 of 6 clean decoys. Run 2026-07-03: spend not metered. This corpus is public and fixed, so the reviewer may have seen it — it measures regression, not liveness; SPEC 2.0 §8.2's generated planted-defect test is tracked separately. Per-defect table: benchmark/results/latest.json.";
