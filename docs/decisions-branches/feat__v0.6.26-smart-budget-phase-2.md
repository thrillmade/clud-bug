## 2026-05-29 23:06 - feat(v0.6.26): Smart Budget Phase 2a — 0.0.W² widen skip + L6 fallback render-from-inlines

**Reasoning:** Two related fixes that ship together. (1) 0.0.W² widens 0.0.W's workflow-only-skip allowlist to cover every file clud-bug update produces (AGENTS.md, .cursorrules/.clinerules/.windsurfrules/.continuerules, .github/copilot-instructions.md, .claude/skills/.clud-bug.json, baseline SKILL.md files, plus logmind side-effect docs). Skip fires only when ALL_IN_ALLOWLIST AND HAS_WORKFLOW_CHANGE — naked AGENTS.md edits still get reviewed (prompt-injection guard). Eliminates the 6-admin-bypass dance we hit across v0.6.24 + v0.6.25 propagation. (2) L6 fallback render-from-inlines: when structured_output is empty BUT inline findings exist (tokenomics #21 pattern), post-step scrapes them via gh api and synthesizes a structured summary. Strict-mode gate anchor still matches; gate falls open advisory. Labeled 'Synthetic summary' so readers know it's reconstructed.

**Alternatives considered:** Combine all 7 layers in one mega-release — rejected: user direction 'ship small changes over and over'. v0.6.26 = 2a (0.0.W² + L6). v0.6.27 = 2b (L5 auto-retry + L3 mid-review check-in)., Ship 0.0.W² without HAS_WORKFLOW_CHANGE requirement — rejected: opens prompt-injection-via-AGENTS.md surface; a malicious AGENTS.md-only PR could ship instructions like 'ignore previous safeguards' and never get reviewed., Include CLAUDE.md in the allowlist — rejected: users frequently edit CLAUDE.md content directly; including it would skip review on substantive CLAUDE.md changes. clud-bug update only touches CLAUDE.md in the 0.0.I.1 block-removal case which is a workflow-change-coupled edit anyway, so this would mostly be redundant safety.

**Implications:**
- Bumps to v0.6.26. Template version v11→v12. strict-mode-gate composite pin v0.6.25→v0.6.26. Updated test 'paths-check classifier: 0.0.W² allow-list + workflow-change signal (v0.6.26+)' covers all the new patterns + the HAS_WORKFLOW_CHANGE = workflow-file mapping + the mixed-diff guard + the AND-both-required logic. 306/306 tests pass. v0.6.26 propagation cycle will be the FIRST one to not require admin-bypass on consumer repos (the propagation PRs themselves will be detected as clud-bug-update-output and auto-skipped).

---
