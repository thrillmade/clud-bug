## 2026-05-26 12:06 - Stream BB.4: skill-first marketing copy refresh (README + npm + site hero)

**Reasoning:** Per the v0.6 plan Stream BB.4: every surface should lead with "ship a brand-voice skill, get brand reviews" — concrete causal claim about what makes clud-bug different. Pre-BB.4 the README opening and npm description described the ARCHITECTURE ("project-aware skills auto-discovered from skills.sh") which is what we built but not what a reader cares about. The site hero subtitle was voice-led ("A field naturalist for your codebase") which is strong voice but vague on value prop. BB.4 reframes the FIRST sentence of each surface around the skill-driven model: README leads with "Ship a brand-voice skill, get brand reviews. Ship a compliance skill, get PII checks." npm description matches. Site hero subtitle becomes "Skills you write. Reviews the bot does." with the field-naturalist binomial preserved as visual signature below.

**Alternatives considered:** Pure voice-led hero (keep "A field naturalist for your codebase"). Rejected: voice was strong but didn't deliver the value prop in the position a reader reads first. The binomial below carries the field-naturalist voice; the subtitle does the elevator pitch. Also considered: leave site alone, only update README + npm. Rejected: the plan explicitly called for hero experiment + the hero is the most-seen surface on cludbug.dev — biggest leverage for the BB.4 framing.

**Implications:**
- Sets the pattern for v0.7 site work — Stream X /install and /pricing pages (post-W.1) will inherit the skill-first framing established here. README first impression is now skill-driven, matching what every dedicated-mode skill installed since v0.5.9 actually does. Bundled as docs-only PR (no version bump); next clud-bug release picks up the new npm description.

---
